import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        borderWidth: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },

    password:{
        flexDirection: 'row', 
        alignItems: "center", 
        marginLeft: 40
    },

    icon: { 
        marginLeft: 10,
    },
})